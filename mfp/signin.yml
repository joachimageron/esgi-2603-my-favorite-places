info:
  name: signin
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5173/api/users/tokens
  body:
    type: json
    data: |-
      {
        "email": "user@example.com",
        "password": "password123"
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
